# Folder Terminal 1.4.4

Bug 修复：复制路径时文件名/目录名停留在「未命名」。

## 🐛 修复

- 图标点击时**重新解析路径**，不再使用注入瞬间固化的旧值
  - 此前 Obsidian 新建文件/目录默认名「未命名」，重命名后文件树的 DOM 节点是就地改名（不重建），图标不会被重新注入，导致点击复制的仍是旧路径「未命名」
- 路径解析优先级调整：优先使用文件浏览器视图的**内部索引 `fileItems`**（新建/重命名/移动后即时同步，权威数据）；DOM 上的 `data-path` 降为兜底（它在重命名后可能仍是旧值）
- 文件行与文件夹行的所有图标（终端 / 复制路径 / 绝对路径 / 标题 / 内容 / 展示）统一生效

## 验证

1. 在 Obsidian 新建一个文件或目录（默认名为「未命名」）
2. 将其重命名为实际名字（DOM 节点不重建）
3. 悬停该行点击「复制路径 / 复制绝对路径」等图标
4. 剪贴板应得到重命名后的真实路径，而非「未命名」