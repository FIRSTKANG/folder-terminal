# Folder Terminal 1.4.3

Bug 修复：手动创建的目录复制路径时误报「目录不存在」。

## 🐛 修复

- 文件夹辅助动作（复制文件路径 / 复制绝对路径 / 系统展示）不再依赖 `vault.getAbstractFileByPath` 反查抽象文件映射
  - 此前手动新建的目录在文件树 DOM 已渲染、但 vault 映射尚未同步时，反查拿不到对象会误报「目录不存在」
  - 现改为直接使用文件树上已解析出的路径执行，与 vault 索引是否同步无关
- 清理不再使用的 `copy.folderNotFound` 词条（中英文）

## 验证

- 在 Obsidian 中手动新建一个目录
- 悬停该目录行，分别点击「复制文件路径 / 复制绝对路径 / 系统展示」三个图标
- 应正常复制路径 / 呼起文件管理器，不再提示「目录不存在」