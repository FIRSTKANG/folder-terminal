# Folder Terminal 1.4.1

修正文件相对路径的复制语义：由去扩展名的「Wiki 路径」改为保留扩展名的「文件系统相对路径」。

## 🐛 修复 / 调整

- **文件「复制文件路径」改为带后缀的相对路径**：此前复制的是去扩展名的相对路径（`wiki/ideas/xxx`），现在复制原始 `file.path`（`wiki/ideas/xxx.md`），可直接用于 `cp`、终端引用等文件系统操作
- **图标提示文案同步**：「复制 Wiki 文件路径」→「复制文件路径」（中英文词条均已更新），文件夹相对路径无扩展名，语义与文案不受影响

## 验证

- 悬停文件点击文件路径图标：复制出 `wiki/ideas/xxx.md`（含扩展名），可直接粘贴到终端用于文件操作
- 悬停文件夹点击文件路径图标：复制出 `wiki/ideas/` 上层的相对目录路径，不含扩展名