# Folder Terminal 1.2.0

Windows 管道模式下的**命令历史**与**行内光标导航**，终端手感对齐真实 shell。

## ✨ 新增

- **命令历史（↑ / ↓）**：Windows 管道模式下 `cmd.exe` 不带 readline，此前方向键序列发过去毫无反应。现在由插件前端维护历史：
  - `↑` 逐条回溯已执行过的命令，`↓` 往回走
  - 到达最新一条后再按 `↓`，会恢复你按 `↑` 之前正在编辑的那半行内容（草稿不丢）
  - 连续重复的命令只记一次；上限 500 条
- **行内光标导航**：`←` / `→` 在已输入内容中左右移动光标；`Home` / `End` 跳到行首 / 行尾（同时支持 CSI 与 SS3 两种转义序列）
- **搜索框支持 ↑ / ↓**：搜索浮层输入框内按 `↑` / `↓` 直接跳到上一处 / 下一处匹配，不必再去点箭头按钮

## 🐛 修复

- 方向键在 Windows 管道模式下"按了没反应"：转义序列原本被透传给 `cmd.exe`，而后者不处理 readline，现改为前端本地接管。

## 🧪 验证

`scripts/lcalecho-test.mjs` 扩充到 13 个仿真用例，覆盖：

- 行编辑回归（退格 / Delete / Ctrl+U / 回车只发整行）
- 命令历史：回溯、到顶不越界、`↓` 回到底部、草稿恢复、导航不向 shell 发字节
- 光标移动：`←` 插入编辑、行首不越界、`Home` / `End`、移动后回车整行完整

全部通过。

## ⚠️ 说明

命令历史为**每个终端标签独立维护**，且仅存在于当前会话（关闭标签即清空，不做跨会话持久化）。macOS / Linux 使用真实 PTY，历史仍由 shell 自身提供，行为不变。
