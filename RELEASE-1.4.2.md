# Folder Terminal 1.4.2

图标语义与一致性优化。

## ✨ 调整

- **「复制文件名」**：复制文件名（`file.basename`，不含扩展名），名称与图标由「标题 + `heading`」改为「文件名 + `file-text`」，更贴合实际行为
- **「复制 Wiki 绝对路径」图标**：`link` → `map-pin`（图钉，传达文件在磁盘上的确切位置）；文件行与文件夹行同步更换
  - 与「文件路径」（`route` 折线）在视觉上形成明确区分：一个表示"走向"，一个表示"定位"
- 图标提示文案同步更新（中英文）

## 验证

- 悬停文件点击文件名图标：复制出文件名（不带扩展名）
- 悬停文件/文件夹点击绝对路径图标：显示图钉图标，复制出磁盘绝对路径
- 文件行与文件夹行的同名动作图标一致