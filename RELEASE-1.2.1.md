# Folder Terminal 1.2.1

搜索功能修复：上下箭头现在能够正常导航到多个匹配结果。

## 🐛 修复

- **搜索框上下箭头导航失效**：修复了搜索日志时，按下箭头每次都跳回第一个匹配结果的问题。
  - 根本原因：`runSearch` 每次调用都会重复执行 `clearDecorations()`，导致 `SearchAddon` 丢失上次匹配位置缓存
  - 修复方案：移除多余的 `clearDecorations()` 调用，让 `SearchAddon` 内部维护搜索状态
  - 现在可以通过 `↑` / `↓` 连续浏览所有匹配项，`SearchAddon` 会自动滚动到激活匹配位置

## 验证

搜索功能验证通过：
- 从第一个结果按 `↓` 能顺序跳到后续每一个匹配
- 到最后一个结果后再按 `↓` 循环回第一个
- `↑` 反向导航行为对称
- 匹配高亮和计数更新正常
